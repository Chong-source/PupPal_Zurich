# For loading Google Maps with GET requests
requests~=2.31.0
# A package that combines google maps and tkinter together
tkintermapview~=1.29
# A package to translate words from German to English
googletrans==4.0.0-rc1
# Used to create user interface
tkinter
# Used to format and read json files as google's API's response is in json
json
